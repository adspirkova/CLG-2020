// Definuj hodnotu casu jako "new Date"


document.addEventListener('DOMContentLoaded', () => {
    // vyber v html dokumentu element podle id 
    // a pridej tomu text (innerText), ktery bude odpovidat hodnote casu

});